# 请求错误

## 400 Bad Request

通常表示请求参数不合法。

检查：

- 模型名称是否正确。
- messages 格式是否符合接口要求。
- 请求体是否包含多余字段。

## 429 Too Many Requests

通常表示请求过快、额度不足或触发限流。

处理：

- 降低并发。
- 稍后重试。
- 检查余额或套餐限制。
- 联系管理员提升额度。

## 500 / 502 / 503

通常表示服务端或上游服务异常。建议保留请求时间、模型名称、错误信息，提交给技术支持排查。
