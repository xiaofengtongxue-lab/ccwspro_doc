# 权限问题

## 文件读写被拒绝

如果工具需要读取或修改本地项目文件，但提示权限不足，请检查：

```bash
ls -la
```

常见处理方式：

- 确认当前用户拥有项目目录权限。
- 避免在系统保护目录内运行工具。
- 检查 IDE 或终端是否有完整磁盘访问权限。

## 工具权限被拒绝

部分 AI 编程工具会对文件操作和命令执行做权限确认。如果曾经选择过「总是拒绝」，需要进入工具的权限管理界面手动恢复。

## 团队权限不足

如果是团队账号，请确认当前成员拥有对应项目、模型和账单权限。
